// Hapanamy.lk Global Cloud Configuration
const CONFIG = {
    // Paste your Supabase credentials here after creating a free project on supabase.com
    SUPABASE_URL: "YOUR_SUPABASE_URL",
    SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY",
    
    // Toggle to true to enable Live Cloud Database (falls back to local storage if false)
    USE_CLOUD_DB: false
};
